 Step 1: Install python-dotenv
bash
Copy
Edit
pip install python-dotenv
📁 Step 2: Create a .env file (store it in the same directory as your script)
dotenv
Copy
Edit
ORACLE_USER=your_username
ORACLE_PASSWORD=your_oracle_password
EMAIL_USER=sender_email@example.com
EMAIL_PASSWORD=your_email_password
⚠️ Add .env to your .gitignore to prevent it from being pushed to GitHub:

bash
Copy
Edit
echo .env >> .gitignore
🛠️ Step 3: Update Your Python Script
At the top of your script, import dotenv and load environment variables:

python
Copy
Edit
from dotenv import load_dotenv
import os

load_dotenv()  # Load variables from .env file

# Fetch credentials securely
user_name = os.getenv("ORACLE_USER")
password = os.getenv("ORACLE_PASSWORD")
email_user = os.getenv("EMAIL_USER")
email_password = os.getenv("EMAIL_PASSWORD")
✅ Updated Password Usage Sections
✅ Oracle DB Connection:
python
Copy
Edit
engine = create_engine(f'oracle+cx_oracle://{user_name}:{password}@{dsn}')
✅ Email Login:
python
Copy
Edit
server.login(email_user, email_password)
And update:

python
Copy
Edit
msg['From'] = email_user
